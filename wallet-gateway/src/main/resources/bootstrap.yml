server:
  port: 8002

spring:
  application:
    name: wallet-gateway
  cloud:
    gateway:
      routes:
        - id: neo_route
          uri: https://www.baidu.com
          predicates:
            - Path=/api